[project]
name = "theoffice-shared"
version = "0.1.0"
description = "Shared types and schemas for theoffice."
requires-python = ">=3.12"
dependencies = ["pydantic>=2.4,<3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/theoffice_shared"]
